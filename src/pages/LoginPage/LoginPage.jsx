import Loader from '../../components/loader/loader';

import './LoginPage.sass'

const LoginPage = () => {
    return(
        <>
            <Loader/>
            <p style={{'fontSize' : '1.5rem'}}>LoginPage</p>
        </>
    )
}

export default LoginPage;